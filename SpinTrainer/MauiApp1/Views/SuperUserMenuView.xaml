<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="SpinningTrainer.Views.SuperUserMenuView"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:mi="http://www.aathifmahir.com/dotnet/2022/maui/icons"
    Title="Super Usuario">
    <Grid BackgroundColor="{StaticResource PrimaryDark}">

        <StackLayout
            Margin="0,250,0,0"
            HorizontalOptions="Center"
            Spacing="15">

            <Button
                Grid.Column="1"
                Margin="10,-40,10,0"
                AbsoluteLayout.LayoutBounds="0.5,1,-1,-1"
                AbsoluteLayout.LayoutFlags="PositionProportional"
                BackgroundColor="#0c192d"
                Clicked="btnOpenSelectionUser_Clicked"
                ContentLayout="Top"
                CornerRadius="40"
                FontAttributes="Bold"
                FontSize="20"
                HeightRequest="250"
                HorizontalOptions="Center"
                ImageSource="{mi:Material Icon=SupervisedUserCircle,
                                          IconSize=180}"
                Text="Gestión de usuarios"
                TextColor="White"
                VerticalOptions="Center"
                WidthRequest="260" />

        </StackLayout>
    </Grid>
</ContentPage>